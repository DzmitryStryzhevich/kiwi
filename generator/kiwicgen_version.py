"""Single source of truth for the kiwicgen version.

The value defined here is consumed by the CLI, Python package metadata and the
standalone executable build. Do not duplicate the version in operational code.
"""

__version__ = "0.1.0"
