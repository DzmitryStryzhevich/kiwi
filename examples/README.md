# Examples

This directory is reserved for practical examples of using the generated component-scoped OSAL API.

The examples will be added after the generic API and its unified primitive semantics are stabilized. They are expected to demonstrate component code that depends only on its generated OSAL contract, without direct FreeRTOS, POSIX, CMSIS-RTOS2 or other native OS calls.
